[project]
name = "morse-trainer"
version = "0.1.0"
description = "CW/Morse code trainer for the R36S handheld"
requires-python = ">=3.10"
dependencies = [
    "pygame-ce>=2.5",
    "numpy>=1.24",
]

[tool.uv]
dev-dependencies = []
